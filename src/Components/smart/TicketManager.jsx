export default function TicketManager({ currentUser }) {
    //logica con funzioni handle per interagire con i ticket come riassegnazioni ecc..
    return (
        <>
            {/* componenti ui per la gestione, renderizzabili in base al ruolo  */}
        </>
    )
}